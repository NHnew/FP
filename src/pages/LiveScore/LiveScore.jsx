import React from 'react';
import Headers from '../../components/Header/Headers';

const LiveScore = () => {
    return (
        <>
            <Headers />
            <div>LiveScore</div>
        </>
    );
};

export default LiveScore;